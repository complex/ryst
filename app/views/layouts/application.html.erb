<!DOCTYPE html>
<html>

  <head>
    <title>R.Y.S.T.</title>
    <%= stylesheet_link_tag 'application' %>
    <!-- <%= stylesheet_link_tag 'dark' if in_game? %> -->
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>

  <body>

    <% if alert %>
      <p><%= alert %></p>
    <% end %>
    <% if notice %>
      <p><%= notice %></p>
    <% end %>

    <nav>
      <ul>
        <li><%= link_to "All scans", scans_path %></li>
        <li><%= link_to "Scans in this system", current_locations_path %></li>
        <li><%= link_to "New scan", new_scan_path %></li>
      </ul>
    </nav>

    <main>
      <%= yield %>
    </main>

  </body>

</html>
